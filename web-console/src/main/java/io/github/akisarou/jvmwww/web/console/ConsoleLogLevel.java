package io.github.akisarou.jvmwww.web.console;

/** Distinct Console Standard printer levels exposed to a host sink. */
public enum ConsoleLogLevel {
    ASSERT,
    DEBUG,
    ERROR,
    INFO,
    LOG,
    WARN,
    COUNT,
    COUNT_RESET,
    GROUP,
    GROUP_COLLAPSED,
    TIME_LOG,
    TIME_END,
    REPORT_WARNING
}
